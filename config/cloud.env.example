# Desktop client (set only after the HTTPS endpoint is deployed)
JARVIS_CLOUD_URL=https://replace-with-container-app.example
JARVIS_CLOUD_API_TOKEN=replace-with-a-random-secret
JARVIS_CLOUD_TIMEOUT_SECONDS=30

# Cloud container
JARVIS_ENV=production
PORT=8000
